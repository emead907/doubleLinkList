/**
 * Possible states of ListIterator that could be tested. 
 * @author CS 221
 */

public enum ListItrState
{
	init, add, nextAdd, nextPrev, nextNextPrev, nextPrevAdd,
	nextPrevRemove, nextNextAdd, nextNextPrevAdd, nextNextPrevRemove, 
	nextNextNextPrev, nextNextNextPrevAdd, nextNextNextPrevRemove, nextNextNextAdd
}
