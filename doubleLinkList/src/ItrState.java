/**
 * Possible states of iterator that could be tested. 
 * @author CS 221
 */
public enum ItrState
{ 
	init, next, nextNext, nextRemove, nextNextRemove, nextRemoveNext,
		nextNextNext, nextNextRemoveNext, nextNextNextRemove
}
